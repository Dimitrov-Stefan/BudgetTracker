@model Web.Areas.Admin.Models.FinancialItems.FinancialItemListViewModel

@{
    ViewData["Title"] = $"Manage Financial items of user {Model.UserName}";
}

<div class="row">
    <div class="col-12 overflow-auto">
        <h1 class="text-center">
            @ViewData["Title"]
        </h1>

        <div class="d-flex justify-content-end">
            <a class="btn btn-primary btn-sm"
               asp-action="@nameof(FinancialItemsController.Create)" asp-route-userId="@Model.UserId">
                <span class="fas fa-plus"></span>
                Add
            </a>
        </div>

        <div class="table-responsive col-md-8 offset-md-2">
            <div id="dataTable_wrapper" class="dataTables_wrapper dt-bootstrap4">
                <table class="table table-hover table-bordered dataTable" id="dataTable" width="100%">
                    <thead>
                        <tr>
                            <th>
                                Name
                            </th>
                            <th>
                                Type
                            </th>
                            <th>
                                Active
                            </th>
                            <th>
                            </th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {

            $("#dataTable").DataTable({
                // Design Assets
                stateSave: true,
                autoWidth: true,
                // ServerSide Setups
                processing: true,
                serverSide: true,
                // Paging Setups
                paging: true,
                // Searching Setups

                searching: { regex: true },
                // Ajax Filter
                ajax: {
                    url: "/Admin/FinancialItems/LoadItemsTable",
                    type: "POST",
                    contentType: "application/json",
                    dataType: "json",
                    data: function (d) {
                        var addValues = [];
                        addValues.push("@Model.UserId.ToString()");
                        d.AdditionalValues = addValues;

                        return JSON.stringify(d);
                    }
                },
                // Columns Setups
                columns: [
                    { data: "name" },
                    {
                        data: "type",
                        render: function (data, type, row) {
                            return data === 0 ? "Expense" : "Income";
                        }
                    },
                    {
                        data: "isActive",
                        render: function (data, type, row) {
                            return data === true ? "Yes" : "No";
                        }
                    },
                    {
                        data: "id",
                        render: function (data, type, row) {
                            return '<div class="text-center"><a class="btn btn-outline-primary btn-sm" href=/Admin/FinancialItems/Edit/' + data + '>Edit</a></div>'; //Hack: Partially hardcoded link
                        }
                    }
                ],
                // Column Definitions
                columnDefs: [
                    { targets: "no-sort", orderable: false },
                    { targets: "no-search", searchable: false },
                    {
                        targets: "trim",
                        render: function (data, type, full, meta) {
                            if (type === "display") {
                                data = strtrunc(data, 10);
                            }

                            return data;
                        }
                    },
                    { targets: "date-type", type: "date-eu" }
                ]
            });
        });
    </script>
}

